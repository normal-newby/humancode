package com.example.humancode.interview;

public enum Phase {
    /** Problem has been delivered, candidate has not typed yet. */
    INTRO,
    CODING,
    /** Solved; the interviewer is asking "now what if...". */
    FOLLOWUP,
    REPORT,
    DONE
}
